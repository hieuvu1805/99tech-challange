// Using arithmetic formula: sum = n * (n + 1) / 2
// Time Complexity: O(1), as it performs only a constant number of operations
// Space Complexity: O(1), as it uses only a few variables
export default function solution3(n: number): number {
  return (n * (n + 1)) / 2;
}